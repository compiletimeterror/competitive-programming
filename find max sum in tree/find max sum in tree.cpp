#include <iostream>
using namespace std;

int findMaxSum(node* root, int& ans){
    if(root == NULL)
        return 0;
    if(root->left == NULL and root->right == NULL)
        return root->data;
   //complete the code
}
